{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["apps/docs/.next/**","!.next/cache/**", "!**/node_modules/**"]
    }
  }
}
